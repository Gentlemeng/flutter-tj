import 'package:flutter/material.dart';

class EssayPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("消息",style: TextStyle(color: Colors.red),),

    );
  }
}
